services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: harrison-knight-molloy-dev
    env_file:
      - .env.local
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
